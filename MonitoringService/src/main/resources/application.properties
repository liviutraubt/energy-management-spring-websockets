spring.application.name=MonitoringService

###############################################
### DATABASE CONNECTIVITY CONFIGURATIONS ###
###############################################
database.ip = ${DB_IP:localhost}
database.port = ${DB_PORT:5432}
database.user = ${DB_USER:postgres}
database.password = ${DB_PASSWORD:root}
database.name = ${DB_DBNAME:monitoringservice}

spring.sql.init.platform=postgres
spring.datasource.url = jdbc:postgresql://${database.ip}:${database.port}/${database.name}
spring.datasource.username = ${database.user}
spring.datasource.password = ${database.password}

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.show_sql=true
server.tomcat.threads.max = 1
###############################################
### LOGGING CONFIGURATIONS ###
###############################################
logging.level.root=warn
logging.level.org.springframework=warn
logging.level.org.example=DEBUG
###############################################
### EMBEDDED TOMCAT CONFIGURATIONS ###
###############################################
spring.rabbitmq.host=kangaroo-01.rmq.cloudamqp.com
spring.rabbitmq.port=5672
spring.rabbitmq.username=uhxqcher
spring.rabbitmq.password=xNCRZ_4PFlv7wXD4AWmpoA07eJwK32of
spring.rabbitmq.virtual-host=uhxqcher
spring.rabbitmq.ssl.enabled=true

application.rabbitmq.queue=${QUEUE_NAME:monitoring_queue}

logging.level.org.springframework.amqp=INFO
logging.level.org.springframework.amqp.rabbit.listener=INFO

server.port=${PORT:8084}